# Flow Debug Build

A CLI tool to download and install Flow Browser debug builds from GitHub Actions artifacts.

## Usage

```bash
npx flow-debug-build <RUN_ID>
```

## Options

- `--open`: Open the installed app
- `--open-folder`: Open the installed folder

## Example

```bash
npx flow-debug-build --open 1234567890
```
